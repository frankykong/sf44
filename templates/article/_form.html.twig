{{ form_start(form, {attr: {'id': 'tinymce_editor', 'data-article-id': article.id} }) }}
    {{ form_widget(form) }}
    <button class="btn btn-sm btn-primary">{{ button_label|default('Save') }}</button>
{{ form_end(form) }}
