{% extends 'adminMenu.html.twig' %}

{% block title %}Edit Article{% endblock %}

{% block body_id 'article_editor' %}

{% block header %}
    {{ parent() }}
    {{ encore_entry_script_tags('editor') }}
{% endblock %}

{% block body %}
    <h1>Edit Article</h1>

    {{ include('article/_form.html.twig', {'button_label': 'Update'}) }}

    <a class="btn btn-sm btn-primary" href="{{ path('article_index') }}">back to list</a>

    {{ include('article/_delete_form.html.twig') }}
{% endblock %}